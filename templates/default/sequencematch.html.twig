{% extends 'base.html.twig' %}
{% block body %}
    <div class="container">
        <h1>Sequence match</h1>
        <br>
        <h4>Sample code : </h4>
        <br>
        {{ code | raw }}
        <br>
        <h4>Result :</h4>
        <div class="row">
            <div class="col-md-10">
                <p>Submatrix :</p>
                {{ dump(submatrix) }}</div>
        </div>
        <div class="row">
            <div class="col-md-10">
                <p>Sequence 1 :</p>
                {{ dump(sequence1) }}</div>
        </div>
        <div class="row">
            <div class="col-md-10">
                <p>Sequence 2 :</p>
                {{ dump(sequence2) }}</div>
        </div>
        <div class="row">
            <div class="col-md-10">
                <p>Computes the Hamming Distance between two sequences</p>
                {{ dump(distance) }}</div>
        </div>
        <div class="row">
            <div class="col-md-10">
                <p>Compares two letters $let1 and $let2 and returns another letter
                    indicating if the two were exact matches, partial matches, or non-matches.<br>
                Not matching (A & T) : </p>
                {{ dump(compare1) }}</div>
        </div>
        <div class="row">
            <div class="col-md-10">
                <p>Matching (A & A) : </p>
                {{ dump(compare2) }}</div>
        </div>
        <div class="row">
            <div class="col-md-10">
                <p>Computes the Levenshtein Distance between two sequences with equal/unequal lengths : </p>
                {{ dump(levdist) }}</div>
        </div>
        <div class="row">
            <div class="col-md-10">
                <p>Extended version of levdist() which accepts strings with length greater than 255 but not to exceed 1024 : </p>
                {{ dump(xlevdist) }}</div>
        </div>
        <div class="row">
            <div class="col-md-10">
                <p>Matching results : </p>
                {{ dump(match) }}</div>
        </div>
    </div>
{% endblock %}